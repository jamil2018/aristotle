# Controlled sample application

`tests/e2e/controlled-sample.spec.ts` intercepts a relative route with
repository-local HTML. It verifies accessible locators, web-first assertions,
deterministic data, cleanup, and low-risk Playwright primitives across Chromium,
Firefox, and WebKit without external infrastructure or production data.
